node_modules/
dist/
*.log
.DS_Store

# Canonical config lives in ~/.config/myx/config.json and holds the iCal secret URL.
# Never commit a real config (treat the iCal URL like a password).
config.json
config.local.json
